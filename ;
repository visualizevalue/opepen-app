<template>
  <section>
    <Card class="static">
      <Progress :percent="demand" :class="{ muted: demand < 50 }" />

      <table>
        <thead>
          <tr>
            <td>Edition</td>
            <td>Holders</td>
            <td>Opt-Ins</td>
            <td>Demand</td>
          </tr>
        </thead>
        <tbody>
          <tr v-for="(edition, name) in EDITION_NAMES">
            <td>{{ name }} {{ edition }}</td>
          </tr>
        </tbody>
      </table>

      <pre>
        {{ submission.submission_stats }}
      </pre>
    </Card>
  </section>
</template>

<script setup lang="ts">
const { submission } = defineProps<{ submission: SetSubmission}>()

const demand = computed(() => getDemandPercentage(submission))
</script>

<style scoped>
.card {
  gap: var(--spacer);
}
</style>
